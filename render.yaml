services:
  - type: web
    name: ai-detector
    env: python
    buildCommand: source deploy.sh
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 8000
    envVars:
      - key: PYTHON_VERSION
        value: 3.12